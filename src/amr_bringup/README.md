# amr_bringup

Top-level launch helpers. Most workflows use package launches directly (`amr_gazebo`, `amr_slam`, `amr_navigation`).

## Run

```bash
ros2 launch amr_bringup display.launch.py
```

Robot model + joint_state_publisher_gui + RViz (no Gazebo).
